
# file holding docker or devel box or any other secret. Exclude it!
*/secrets/**
# this may be for secrets at level where git push is run?
secrets/**

# after removing an entry from this file, must remove local cache with "git rm -r --cached ."
# or else it will still get saved & updated
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# WARNING .eslintcache, even if excluded in .gitignore causes node compilation about about
#    problems which no longer exist, but are cached. So delete this file if that happens.
.eslintcache
*.nogit

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
